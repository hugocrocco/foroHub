package com.foro.forohub.security;

// LEGACY placeholder: intentionally empty. The active security setup lives in
// `com.foro.forohub.config.SecurityConfig`.
public class SecurityConfig {
    // Intentionally empty.
}